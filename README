Got it — you want a clean, standard Markdown README that is fully copyable, with no special blocks, no hidden formatting, no IDs, no custom wrappers.

Here is the corrected version 👇 — you can copy everything directly into your README.md.

⸻

🚀 End-to-End CI/CD Pipeline for PHP App on Azure

(Azure CLI + Azure DNS + GitHub Actions + Zero-Downtime Deployment)

⸻

📌 Project Overview

This project demonstrates a production-ready CI/CD pipeline for deploying a PHP web application using:

* Azure App Service (Linux PaaS)
* Azure DNS (Custom Domain Management)
* GitHub Actions (CI/CD Automation)
* Azure CLI (Infrastructure Provisioning)

The system enables fully automated deployments, custom domain integration, and secure HTTPS access with zero downtime.

⸻

🧭 Real-World Scenario

At Your Techie Hub, deployments were:

* Manual
* Slow
* Error-prone

The business required:

* Automatic deployment on every push
* Custom domain (www.auemeribetech.com.ng)
* HTTPS security
* No server management

This project solves these problems using modern DevOps practices.

⸻

🎯 Objectives

* Deploy a PHP application on Azure App Service (Linux)
* Automate deployments using GitHub Actions
* Manage DNS using Azure DNS
* Enable HTTPS with managed SSL
* Achieve zero-downtime deployments using staging slots

⸻

🏗️ Architecture

![Architecture](docs/architecture-diagram.png)

⸻

🧰 Technologies Used

* Microsoft Azure App Service (Linux)
* Azure DNS
* GitHub Actions
* Azure CLI
* PHP
* Git and GitHub

⸻

📁 Project Structure

elearning-php-ci-cd/
├── .github/
│   └── workflows/
│       └── deploy.yml
├── docs/
│   ├── architecture.dot
│   └── architecture-diagram.png
├── css/
├── img/
├── js/
├── lib/
├── scss/
├── index.php
├── about.php
├── contact.php
├── courses.php
├── team.php
├── testimonial.php
├── 404.php
├── LICENSE.txt
├── README.md
├── .gitignore
└── .gitattributes

⸻

⚙️ Deployment Workflow

Code Push ->
   Build ->
      Package ->
         Deploy to Staging ->
            Slot Swap ->
               Production (Zero Downtime)

⸻

🚀 Azure CLI Setup

Create Resource Group

az group create --name elearning-rg --location westus3

Create App Service Plan

az appservice plan create \
  --name elearning-plan \
  --resource-group elearning-rg \
  --sku B1 \
  --is-linux

Create Web App

az webapp create \
  --resource-group elearning-rg \
  --plan elearning-plan \
  --name elearning-app-12345 \
  --runtime "PHP|8.2"

Create Staging Slot

az webapp deployment slot create \
  --name elearning-app-12345 \
  --resource-group elearning-rg \
  --slot staging

⸻

🌐 Azure DNS Configuration

Create DNS Zone

az network dns zone create \
  --resource-group elearning-rg \
  --name auemeribetech.com.ng

Add CNAME Record

az network dns record-set cname set-record \
  --resource-group elearning-rg \
  --zone-name auemeribetech.com.ng \
  --record-set-name www \
  --cname elearning-app-12345.azurewebsites.net

Add TXT Record

az network dns record-set txt add-record \
  --resource-group elearning-rg \
  --zone-name auemeribetech.com.ng \
  --record-set-name asuid.www \
  --value "<verification-id>"

Bind Custom Domain

az webapp config hostname add \
  --webapp-name elearning-app-12345 \
  --resource-group elearning-rg \
  --hostname www.auemeribetech.com.ng

⸻

🔐 GitHub Secrets

gh secret set AZURE_WEBAPP_NAME --body "elearning-app-12345"
gh secret set AZURE_WEBAPP_PUBLISH_PROFILE_PROD < prod-profile.xml
gh secret set AZURE_WEBAPP_PUBLISH_PROFILE_STAGING < staging-profile.xml
gh secret set AZURE_CREDENTIALS

⸻

⚙️ CI/CD Workflow (Enterprise Pipeline)

Location: .github/workflows/deploy.yml

Stages:

* Build and Package
* Deploy to Staging
* Swap to Production

name: Enterprise CI/CD - PHP App
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: zip -r app.zip .
    - uses: actions/upload-artifact@v3
      with:
        name: php-app
        path: app.zip
  deploy-staging:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - uses: actions/download-artifact@v3
    - uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
        slot-name: staging
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_STAGING }}
        package: app.zip
  swap-to-production:
    runs-on: ubuntu-latest
    needs: deploy-staging
    steps:
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
    - run: |
        az webapp deployment slot swap \
          --name ${{ secrets.AZURE_WEBAPP_NAME }} \
          --resource-group elearning-rg \
          --slot staging \
          --target-slot production

⸻

🌐 Live Application

https://www.auemeribetech.com.ng

⸻

❌ Common Issues & Fixes

Issue	Solution
Domain not resolving	Check DNS records and wait
Cannot add domain	Upgrade App Service plan
HTTPS not working	Bind SSL certificate
CI/CD not triggering	Check GitHub Actions logs

⸻

🧹 Cleanup

az group delete --name elearning-rg --yes --no-wait

⸻

🧠 Key DevOps Learnings

* CI/CD removes manual deployment risks
* PaaS simplifies infrastructure
* Azure DNS enables automation
* Deployment slots ensure zero downtime

⸻

🏁 Final Outcome

* Fully automated CI/CD
* Zero downtime deployment
* Secure HTTPS
* Production-ready architecture

⸻

👤 Author

Anthony Uchenna Emeribe
Cloud / DevOps Engineer

⸻

⭐ Support

If you found this helpful, consider giving it a star on GitHub.

⸻

✅ This version fixes:

* No hidden formatting
* No special block wrappers
* No problematic characters near the image
* Proper GitHub Markdown rendering

⸻

If the image still doesn’t show after this, the issue is 100% the file path or image itself, not the README anymore.